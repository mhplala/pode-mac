# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode user-local state (commit the .xcodeproj itself, ignore per-user bits)
**/xcuserdata/
**/*.xcuserstate
**/UserInterfaceState.xcuserstate
*.moved-aside
*.swp
*.hmap

# Build / derived
DerivedData/
build/
*.app
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# IDE
.idea/
.vscode/

# Logs / scratch
*.log
*.tmp

# AppIcon source — handed to scripts/build-dmg.sh on demand,
# the generated AppIcon set is what lives in the repo.
icon-source.png
